import HeroSection from "../components/HeroSection";

const About = () => {
  const data = {
    name: "Saha Ecommarce",
  };

  return (
    <>
      <HeroSection myData={data} />
    </>
  );
};

export default About;
